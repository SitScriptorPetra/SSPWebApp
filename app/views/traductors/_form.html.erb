<%= form_with(model: traductor) do |form| %>
  <% if traductor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(traductor.errors.count, "error") %> prohibited this traductor from being saved:</h2>

      <ul>
        <% traductor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_field :correo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
